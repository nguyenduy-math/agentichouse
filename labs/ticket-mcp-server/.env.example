# Path to SQLite database file. Defaults to ./data/tickets.db if not set.
TICKET_DB_PATH=./data/tickets.db

# Default priority for new tickets if not specified by the caller
TICKET_DEFAULT_PRIORITY=normal

# Timezone for display (informational — server stores UTC internally)
DEFAULT_TIMEZONE=Asia/Ho_Chi_Minh
